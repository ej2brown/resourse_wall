<header>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01"
      aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
      <a class="navbar-brand" href="/"><span class="logo-highlight">Resource Wall</span></a>
      <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
        <form action='/resources/addResource' method='GET'>
          <button class="nav-link"> Upload A Resource <span class="sr-only">(current)</span></button>
        </form>
        <form action='/categories' method='GET'>
          <button class="nav-link"> Categories <span class="sr-only">(current)</span></button>
        </form>
        <form action='/users/profile' method='GET'>
          <button class="nav-link"> My Profile <span class="sr-only">(current)</span></button>
        </form>


      </ul>

      <!-- Functionality: if a user isn't logged in, display the form below -->
      <form class="form-inline my-2 my-lg-0" method='GET' action='/resources/search' name="searchText">
        <input class="form-control mr-sm-2" type="search" placeholder="Search title/category" aria-label="Search" name='search'
        action='/resources/search'>
        <button class="btn btn-outline-success my-2 my-sm-0" type="submit">
        </button>
      </form>
      <!-- Login and Register will show if no users are currently logged in -->
      <!-- On click, it will route them to the correct page -->
      <form method='GET' action='/users/login' class="form-inline my-2 my-lg-0">
        <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Login</button>
      </form>

      <form method='GET' action='/users/register' class="form-inline my-2 my-lg-0">
        <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Register</button>
      </form>

      <form method='GET' action='/users/logout' class="form-inline my-2 my-lg-0">
        <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Logout</button>
      </form>

    </div>
  </nav>
</header>
